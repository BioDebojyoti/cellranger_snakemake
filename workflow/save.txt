        if [[ -s {output.flag} ]]; then 
            echo $dir2remove >> logs/mkfastq.log; 
            find {params.outdir2use}/$dir2remove -iname '*.gz' | xargs -I {{}} mv {{}} {params.outdir2use}; 
        else
            echo "Error: Success message not found in the log." >&2;
            exit 1
        fi

       os.path.join(outdir, "mkfastq.success")


       